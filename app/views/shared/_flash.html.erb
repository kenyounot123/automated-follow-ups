<% flash.each do |type, message| %>
  <div class="mb-4 rounded-lg border px-4 py-3 text-sm <%= type.to_s == "alert" ? "border-red-200 bg-red-50 text-red-800" : "border-brand-100 bg-brand-50 text-brand-800" %>">
    <%= message %>
  </div>
<% end %>
